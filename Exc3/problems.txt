1. Синхронный запрос, который может не влезить по времени/памяти и рамки, отведенные для запроса и будет отброшен. Соответственно, остальные сервисы останутся без обновленных данных. (+ время на обработку данных для агрегации + время когда каждый из 5ти партнерских сервисов вернет ответ). А если одна из компаний изменила API и никому про это не сказала? Тут, конечно, не очень понятно, как обрабатываются данные, в случае, если один из партнеров вернул то, что приведёт к ошибке обработки?
2. Потенциальный рассинхрон данных между репликами в сервисах core-app и ins-comp-settlement и оригиналом в ins-product-aggregator.
3. Частые запросы (раз в 15 минут) из core-app в ins-product-aggregator (А зачем так часто? Там какая-то из страховых штампует новые условия страховок раз в 15 минут?) потенциально нагружают core-app (который и так монолит) довольно жирным запросом.
4. А если ночью запрос ins-comp-settlement упалс ошибкой то что?
5. Если мы делаем объемную запись в реплику на основании импорта данных, то у нас база данных может стать слабым звеном для чтения данных дригими запросами при большой числе клиентов. Хотя опять же вопрос к релизации. Врядли страховые компании раз в 15 минут удаляют все свои продукты добавляют новые. Разумнее было бы записывать дельту между текущими и новыми данными.
